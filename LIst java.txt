A list is a collection tt:
 	stores multiple values
	maintains insertion order 
	allows duplicate elements
List<Integer> lst = new ArrayList<>();
	lst.add(index,element)
	lst.remove(index);
	lst.get(index);
	lst.size();
	lst.set(index,element);
	lst.contains(element);
	lst.isEmpty();
	
	
	
LinkedList<Integer> lst  = new nkedList<>();









HahMap<Integer,Integer> sol = new HashMp<>();
	sol.add(element);
	sol.get(


	